{
  "name": "delizia-sistema-gestion",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build",
    "install": "cd frontend && npm install && cd ../backend && npm install",
    "start": "cd backend && npm start",
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm start\""
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "dependencies": {
    "mongoose": "^8.16.0",
    "node-fetch": "^3.3.2"
  }
}
